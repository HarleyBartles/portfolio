# CI validation pipeline

Every repo that uses `repo-standards` should have a `tools/run.py` task runner
that composes the same checks CI runs for that repo.

- The `ci` task is the full verification surface. `ci --apply` applies mechanical
  surfaces only. `ci --check` is the fail-fast CI/PR gate. `ci --check --diagnostics`
  reports every independent failing target without stopping at the first.
- The tracked pre-commit hook is the single complete local gate for a normal
  commit. It materializes the staged snapshot, runs `ci --apply`, stages only
  the owned generated surfaces, then runs `ci --check --diagnostics` on that
  snapshot before allowing the commit.
- CI runs the same `ci --check` target in a fail-fast workflow.

## Preflight: `tools/run.py ci --check`

`ci --check` is the full non-mutating CI/PR gate. It runs every dependency in
`ci` order (`lint`, `repo-standards`, `validate`, `archive-links`) and stops at
the first failure. Add `--diagnostics` to collect and report every independent
failure before the command exits.

Run the smallest focused checks that prove the slice while iterating. For a
normal commit, stage the intended tree and let the tracked pre-commit hook be
complete local gate.

## Pre-commit hook: `.git/hooks/pre-commit`

The hook is generated by `repo-standards` and:

1. preserves and hides all unstaged edits and untracked files;
2. materializes the staged index in the working tree;
3. runs `py -3 tools/run.py ci --apply --allow-shared-checkout`;
4. stages only the owned generated surfaces it produced;
5. runs `py -3 tools/run.py ci --check --diagnostics` on the exact staged snapshot;
6. restores the user's unstaged and untracked work.

If any step fails, the commit is blocked. Do not bypass the hook with `--no-verify`.

## CI workflow

CI composes the same checks as separate workflow steps. Each step has its own
label so failures are easy to attribute. CI does not call the preflight script;
it runs the same commands directly.

## What to do when a check fails

A failing `ci --check --diagnostics` run lists every failing target together
with the repair command. For example:

- lint -> `py -3 tools/run.py lint --apply`
- repo shape -> `py -3 tools/run.py repo-standards --apply`
- agent mesh -> `py -3 tools/run.py mesh --apply`
- marketplace drift -> `py -3 tools/run.py marketplace --apply`
- archive links -> `py -3 tools/run.py archive-links --apply`

Run the repair command for each failing target, then re-run only that target's
focused `--check` to confirm it is green before committing again:

- `py -3 tools/run.py lint --check`
- `py -3 tools/run.py repo-standards --check`
- `py -3 tools/run.py mesh --check`
- `py -3 tools/run.py marketplace --check`
- `py -3 tools/run.py archive-links --check`

Do not run the full `py -3 tools/run.py ci --check` immediately before a normal
commit or immediately after a successful hooked commit. The hook already proves
the staged tree.

## When to run the complete gate manually

Run the complete `py -3 tools/run.py ci --check` only in these cases:

- no normal commit will follow (a one-shot uncommitted verification),
- you are diagnosing the pipeline and need a complete fail-fast report, or
- you are explicitly proving CI parity.

## Template menu

- `py -3 tools/run.py ci --apply` (apply mechanical surfaces)
- `py -3 tools/run.py ci --check --diagnostics` (pre-commit style, multi-failure)
- `py -3 tools/run.py ci --check` (CI parity, fail-fast)
- `py -3 .agents/skills/repo-standards/scripts/repo_standards.py --check`
- `py -3 .agents/skills/repo-standards/scripts/scaffold-all.sh --check`
- `py -3 .agents/skills/generating-agent-mesh/scripts/validate_agent_mesh.py --check`
- `py -3 .agents/skills/refreshing-installed-skills/scripts/refresh_installed_skills.py --check`
- repo-specific lint command
- repo-specific final CI command
