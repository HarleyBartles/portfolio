version: 1
metadata:
  skill_name: generating-images
  source_category: skills-with-source
interface:
  display_name: Generating Images
  short_description: Master current OpenAI image tools across Codex and ChatGPT.
  default_prompt: Use generating-images to adapt an authored visual brief to the current OpenAI image surface, bind references deliberately, inspect the actual result, and report truthful custody and limitations.
policy:
  products:
  - codex
  - chatgpt
  allow_implicit_invocation: true
