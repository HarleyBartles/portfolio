version: 1
metadata:
  skill_name: typescript
  source_category: first_party
interface:
  display_name: TypeScript
  short_description: Guidance for writing or reviewing TypeScript type design, generics, module resolution, or compiler configuration.
  default_prompt: Use typescript when writing or reviewing TypeScript type design, generics, module resolution, or compiler configuration.
policy:
  allow_implicit_invocation: true
