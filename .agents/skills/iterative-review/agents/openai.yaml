version: 1
metadata:
  skill_name: iterative-review
  source_category: first_party
interface:
  display_name: Iterative Review
  short_description: Human-approved legacy review assistance for non-frontier orchestrators.
  default_prompt: Use iterative-review only after explicit human approval to assist a non-frontier orchestrator with legacy draft-PR review; do not use it with Sol or another harness-designated frontier model.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: false
