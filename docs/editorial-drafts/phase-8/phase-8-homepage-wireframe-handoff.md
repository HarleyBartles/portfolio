# Phase 8 homepage wireframe handoff

**Status:** Ready for Cloud/Harley wireframe re-inspection. The bounded return pass is now folded into the disposable composition proof. This remains a discovery artifact inside the active homepage design room, not a production implementation plan, and does not authorize edits to the production `HomePage`, shared production styles, routing, tests or edition mechanics.

The static composition proof is [`phase-8-homepage-wireframe.html`](./phase-8-homepage-wireframe.html).

## What is now standing

1. Fold 1 retains the locked identity, thesis and four-item proof rail, and its down-link now lands on an authored clean start for Fold 2.
2. Fold 2 keeps the accepted deterministic modular-channel language but gives the proof materially stronger scale, a hard frame and a shallow authored crop. Its narrow source order is label/title, proof, body, inward route and down-link.
3. Fold 3 now proves the immutable-history topology explicitly: events only emit; the leftward reads enter a replay spine; the rightward reads converge on Cache; Replay and Cache both terminate at the same State. All diagram labels are real SVG text, and the narrow asset is a deliberate vertical recomposition.
4. Writing remains the accepted typographic decompression movement; only its inward/down-link grammar and native landing plate changed.
5. The accepted `PATCH / The Usual Specialists` movement keeps its internal type, copy and art direction. At intermediate and narrow widths, the Silk, folder, Rollback and Receipt carriers now use unequal widths and opposing edge pressure; Klause remains a true zero-flow overprint across the folder/Rollback seam.
6. The reciprocal professional close is unchanged apart from the agreed inward-route arrow grammar.

## Fold 2 disposition

The stand-in clears its ten-second job without becoming the full side quest: a calm inherited system is visible first; one stronger, close-tolerance intervention changes the route; the continuation that intervention enables is visible in the same frame. Its wide, intermediate and narrow assets are authored for their fields rather than reduced screenshots. The supplied full-resolution moodboard is retained as exploration authority, not traced as a production asset.

## Rendered return evidence

The complete page was inspected locally at:

- `1440 × 1000`
- `768 × 900`
- `390 × 844`
- `320 × 844`

The selected Fold 2 and Fold 3 assets switch at their intended widths; all referenced media decoded successfully; and no page-level horizontal overflow remains at any review width. Every native down-link was exercised by click and landed its target at the same authored `28px` datum without scroll interception. Semantic `PATCH` and `The Usual Specialists` text remains in the DOM. `?media=off` hides the Patch image choreography while preserving its understandable fallback and semantic title. Reduced-motion mode resolves to native `auto` scrolling. A browser-level `200%` zoom pass retained semantic content and introduced no horizontal overflow.

Klause's overprint was checked separately at `768`, `390` and `320px`: removing it changes neither the Patch movement height nor the Rollback/Receipt offsets. At all three widths it intersects both the folder plate and Rollback carrier, proving the accepted seam-crossing while remaining zero-flow.

## Re-inspection questions

- Does Fold 1 establish sufficient professional authority without exhausting the page before the work begins?
- Does the stronger, hard-framed Fold 2 now read as serious use changing a strong base system without becoming a second side quest?
- Does Fold 3's exact event-to-Cache/replay-to-State topology read in ten seconds and break the agentic-portfolio rhythm with recognisably conventional engineering proof?
- Does Writing create enough decompression before Patch returns the page to spectacle?
- Do the transitions around the approved Patch movement preserve its theatricality without making the neighbouring movements feel unfinished?
- Does the reciprocal close feel earned and direct?

## Hard stop

Do not continue into production homepage components, routing, edition selection, animation, publication or final asset commissioning from this handoff. Cloud/Harley rendered re-inspection is the next gate.
