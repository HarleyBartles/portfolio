# Phase 8 homepage wireframe handoff

**Status:** Ready for Cloud/Harley Fold 3 topology re-inspection. The bounded event-sourcing correction is now folded into the disposable composition proof. This remains a discovery artifact inside the active homepage design room, not a production implementation plan, and does not authorize edits to the production `HomePage`, shared production styles, routing, tests or edition mechanics.

The static composition proof is [`phase-8-homepage-wireframe.html`](./phase-8-homepage-wireframe.html).

## What is now standing

1. Fold 1 remains compositionally unchanged.
2. Fold 2 now breaks in from the right as a materially larger, tighter crop without a hard image border. The mineral copy plane overlaps and crops the tile field instead of leaving image and copy as polite adjacent modules. Its narrow visual sequence remains label/title, proof, body, inward route and down-link; the semantic source keeps the explanatory copy and routes before the presentation image.
3. Fold 3 answers from the left with the corrected topology: the normal path is `events → Cache → State`; the rebuild path is `complete event history → Replay → Cache → State`. Replay never writes directly to State. At wide widths, the State/fade region still passes beneath the right-side claim and acts as the seam between evidence and interpretation. Tablet now has its own condensed horizontal plate, while mobile uses a deliberate vertical recomposition whose State dissolves downward.
4. Writing is compositionally unchanged and retains its authored jump viewpoint.
5. The accepted `PATCH / The Usual Specialists` movement keeps its type, copy, assets and sequence. At intermediate and narrow widths, Silk, folder, Rollback and Receipt now press and overlap the page edges with less regular spacing and more deliberate comic-panel placement; Klause remains a true zero-flow overprint across the folder/Rollback seam.
6. The reciprocal professional close remains compositionally unchanged.

## Fold 2 disposition

The stand-in clears its ten-second job without becoming the full side quest: a calm inherited system is visible first; one stronger, close-tolerance intervention changes the route; the continuation that intervention enables is visible in the same field. The new crop crosses all four edges of its visual window at every review width, and the image no longer presents as a bordered object. Its wide, intermediate and narrow assets remain authored for their fields rather than reduced screenshots. The supplied full-resolution moodboard is retained as exploration authority, not traced as a production asset.

## Fold 3 topology disposition

The six named records remain one canonical immutable ordered history and only emit data. Their warm live-refresh routes converge on the replaceable Cache during normal processing. The darker complete-history spine consumes the whole ordered record and enters Replay; Replay's only output is the explicit `REBUILD CACHE` route. Cache is the sole input to State. The former Cache/Replay parallel-to-State model and its `EQUAL` label are retired.

Desktop preserves the accepted left-to-right authority and rightward State degradation. Tablet uses a separately authored horizontal SVG with shorter connector travel and substantial Cache, Replay and State objects rather than a uniformly reduced desktop plate. At `720px` and below, the vertical asset resets the horizontal left bleed, separates full-history and live-refresh spines on opposite sides of the event stack, converges them at Cache, and degrades State downward into the following explanation.

## Rendered return evidence

The complete page was inspected locally at:

- `1440 × 1000`
- `768 × 900`
- `390 × 844`
- `320 × 844`

The selected Fold 2 and Fold 3 assets switch at their intended widths; all referenced media decoded successfully; and no page-level horizontal overflow remains at any review width. Fold 3 was inspected at `1440`, `901`, `900`, `801`, `800`, `768`, `721`, `720`, `640`, `390` and `320px`. The `720/721px` horizontal-to-vertical boundary has an explicit positioning reset: the vertical proof begins at the viewport edge with no inherited negative margin or oversized horizontal image. The `800/801` and `900/901px` transitions also preserve the corrected topology and readable event labels. At `1440px`, Fold 3's right-side claim still begins inside the rendered State/fade region, so the accepted State surface remains the actual seam rather than a decorative gap.

Every native down-link was exercised by click and landed on its declared target without scroll interception. Keyboard traversal reaches the page navigation and movement links in source order. Semantic `PATCH` and `The Usual Specialists` text remains in the DOM. Media-off inspection preserves the three movements' understandable document structure. Reduced-motion mode resolves to native `auto` scrolling. A `200%` zoom pass retained semantic content and introduced no horizontal overflow. The invitation remains a single line at all four established review widths.

Klause's overprint was checked separately at `768`, `390` and `320px`: removing it changes neither the Patch movement height nor the Rollback/Receipt offsets. At all three widths it intersects both the folder plate and Rollback carrier, proving the accepted seam-crossing while remaining zero-flow.

## Re-inspection questions

- Does Fold 1 establish sufficient professional authority without exhausting the page before the work begins?
- Does Fold 2's right-side breakout and mineral-plane crop now read as serious use changing a strong base system without becoming a second side quest?
- Does Fold 3 now say, without paragraph-level explanation, that live events maintain Cache, full-history Replay rebuilds that same Cache, and Cache alone produces State?
- Does Writing create enough decompression before Patch returns the page to spectacle?
- Do the transitions around the approved Patch movement preserve its theatricality without making the neighbouring movements feel unfinished?
- Does the reciprocal close feel earned and direct?

## Hard stop

Do not continue into another homepage refinement or into production homepage components, routing, edition selection, animation, publication or final asset commissioning from this handoff. Cloud/Harley Fold 3 topology re-inspection is the next gate.
