# Phase 8 homepage wireframe handoff

**Status:** Ready for Cloud/Harley Fold 3 material/composition re-inspection. The event-sourcing proof now has a resolved material hierarchy, an intentional State/editorial seam and one coherent breakpoint strategy inside the disposable composition wireframe. This remains a discovery artifact inside the active homepage design room, not a production implementation plan, and does not authorize edits to the production `HomePage`, shared production styles, routing, tests or edition mechanics.

The static composition proof is [`phase-8-homepage-wireframe.html`](./phase-8-homepage-wireframe.html).

## What is now standing

1. Fold 1 remains compositionally unchanged.
2. Fold 2 now breaks in from the right as a materially larger, tighter crop without a hard image border. The mineral copy plane overlaps and crops the tile field instead of leaving image and copy as polite adjacent modules. Its narrow visual sequence remains label/title, proof, body, inward route and down-link; the semantic source keeps the explanatory copy and routes before the presentation image.
3. Fold 3 answers from the left with one semantic DOM/CSS proof rather than three diagram images. The normal path is `events → Cache → State`; the rebuild path is `complete event history → Replay → Cache → State`. Replay never writes directly to State. Ordinary site-native history feeds restrained Cache/Replay mechanisms; the generated desert State surface is the expressive culmination. Expansive desktop lets the locked headline inhabit the State fade, compact landscape and tablet use one consistent left article rail, and narrow becomes a deliberate vertical evidence sequence.
4. Writing is compositionally unchanged and retains its authored jump viewpoint.
5. The accepted `PATCH / The Usual Specialists` movement keeps its type, copy, assets and sequence. At intermediate and narrow widths, Silk, folder, Rollback and Receipt now press and overlap the page edges with less regular spacing and more deliberate comic-panel placement; Klause remains a true zero-flow overprint across the folder/Rollback seam.
6. The reciprocal professional close remains compositionally unchanged.

## Fold 2 disposition

The stand-in clears its ten-second job without becoming the full side quest: a calm inherited system is visible first; one stronger, close-tolerance intervention changes the route; the continuation that intervention enables is visible in the same field. The new crop crosses all four edges of its visual window at every review width, and the image no longer presents as a bordered object. Its wide, intermediate and narrow assets remain authored for their fields rather than reduced screenshots. The supplied full-resolution moodboard is retained as exploration authority, not traced as a production asset.

## Fold 3 topology disposition

The six named records remain one canonical immutable ordered history and only emit data. Event names carry the primary narrative hierarchy; `EVENT nn · HH:MM:SS` sits above each as quieter ordering metadata. The times are illustrative proof data, not captured session timestamps. Six lighter individual routes visibly maintain the replaceable Cache during normal processing. One materially heavier complete-history sweep consumes the whole ordered record and enters Replay; Replay then rebuilds that same Cache. Cache is the sole input to State. No helper captions label the routes; their contrasting construction carries the distinction.

Every route is built around its receiving edge. Arrowhead length and span are proportional to route weight, each head is centred on its shaft, and its tip terminates on the border of the object it feeds. The heavy history sweep lands on Replay; Replay crosses only the process gutter into Cache; the six live routes terminate independently on Cache; Cache alone terminates on State.

At `1280px` and above, the governing proportional construction remains lateral: the event stack and Cache share exact top and bottom datums; Replay adds one small gutter and one lower process box; the history sweep occupies the corresponding open depth beneath the events; and State spans the complete Cache/gutter/Replay height. State begins at the `40%` page datum and owns the remaining `60%`, so it is the fold's dominant visual plane. Project metadata sits above the proof on clean mineral ground. The locked headline occupies the upper clear mineral fifth where the State surface recedes. The précis and routes form one slightly opaque mineral intrusion in the lower fade: it sits wholly inside the imagery at full desktop, while the tighter `1280–1399px` regime deliberately crosses the lower State edge rather than pretending the same depth remains available.

From `901–1279px`, the same lateral proof topology remains, with State beginning at the `50%` datum rather than being compressed into a right-side panel. Metadata leads on the left, while the confidently scaled headline sits lower inside the State fade rather than leaving that editorial territory unused; it remains in the contracted Source Sans 3 project voice rather than borrowing the Source Serif 4 authored-article register. The précis and routes return to the left article rail beneath the proof. From `721–900px`, events and Cache form the two-column upper assembly, Replay remains beneath Cache, the full-history sweep occupies the open depth beneath the events, and a separate southbound Cache route enters a State plane spanning both columns. The article rail remains on the left. At `720px` and below, metadata and headline lead, the diagram becomes the evidence payoff, Replay and Cache form a measured process row, and the State plane receives the final downward route. The précis and routes then occupy one centred, slightly opaque mineral reading block whose top is fixed to the State plane's vertical midpoint. That block occupies `90%` of the image width, leaving `5%` image gutters on both sides so the authored fade remains visible, and it deliberately crosses the bottom edge by at least `20px`; breakout is no longer an accident of text reflow. Each live event first exits cleanly to the right, then occupies its own rail and terminates at a distinct point across the Cache edge; no route crosses another event block.

State carries the restrained Wild Bunch material note. A generated abstract mineral/heat-haze texture lives only inside the State plane; it is not a literal desert illustration and carries no baked labels. The proof preserves the texture's complete authored dark-to-haze frame rather than zooming away its recession; CSS only completes the short terminal handoff into the exact mineral shell colour. The mechanically rotated derivative preserves that same authored recession in the southbound compositions. Live DOM fragments — `Player`, `World`, `Clock`, `Journey`, `Case file` and `Pursuit` — travel diagonally toward the lightening horizon. At expansive desktop they deliberately clear the headline's fade territory. The final fragments lose contrast progressively as they travel into the haze, making staleness part of the composition rather than introducing a new sharp ink colour at the edge.

The material progression is now explicit. Event cards use calm portfolio whites and cool-grey infrastructure. Replay carries a near-black, softly grained leather treatment over a dark base. Cache carries a pale dry-woven crosshatch over a straw/mineral base. Both mechanism boxes now carry only their essential names; explanatory scaffold phrases were removed because the routes already prove their roles. State remains the only openly expressive surface, and its visible value stops at `Current view` rather than leaking an implementation member name. Replay and Cache textures were generated as decorative progressive enhancement; removing every generated image leaves the same hierarchy, labels and topology intact.

The six event-to-trunk contacts are square connector nodes wider than the trunk, not arrowheads. Directional arrowheads exist only on actual flows, remain proportionate to their shafts and terminate on the receiving border.

The three retired breakpoint SVGs were removed. Responsive layout now acts on one semantic object, and the textual proposition precedes that object in source order.

## Rendered return evidence

The complete page was inspected locally at the five standing review widths:

- `1440 × 1000`
- `984 × 912`
- `768 × 900`
- `390 × 844`
- `320 × 844`

The selected Fold 2 asset still switches at its intended widths; both State texture orientations and the new Replay/Cache textures decoded successfully; and no page-level horizontal overflow remains at any review width. Fold 3 was additionally swept at `1600`, `1440`, `1400`, `1399`, `1280`, `1279`, `1200`, `1199`, `1100`, `1024`, `901`, `900`, `820`, `801`, `800`, `768`, `721`, `720`, `640`, `520`, `480`, `390`, `360`, `340` and `320px`. The `1399/1400px` boundary changes the mineral reading block from a deliberate lower-edge breakout to a fully contained State inset. The `1279/1280px` boundary deliberately changes between the single left article rail and the expansive State-seam composition. The `900/901px` boundary changes the proof from a lateral three-column assembly to the two-column/southward State composition. The `720/721px` boundary changes to the authored vertical evidence sequence. In every regime there is exactly one proof, six live routes and one complete-history route; event names remain readable; `TIMESTAMP` does not appear; and the relevant texture assets decode.

Rendered geometry checks show a `0px` event-stack/Cache height delta throughout the wide and intermediate regimes. At wide widths, State height matches the complete Cache-to-Replay process height within `0.1px`. The desktop reading block is fully contained from `1400px` upward and intentionally crosses the State edge from `1280–1399px`. At `720px` and below, its width is exactly `90%` of the State plane, its top equals the State midpoint, the texture fade remains visible in both `5%` gutters, and its bottom clears the image edge by at least `20px` throughout the regime.

Every native down-link was exercised by click and landed on its declared target without scroll interception. Keyboard traversal reaches the page navigation and movement links in source order. Semantic `PATCH` and `The Usual Specialists` text remains in the DOM. Media-off inspection removes all three generated Fold 3 surfaces while preserving the ordinary-history → restrained-mechanisms → expressive-State hierarchy through base colour, border and live text. Reduced-motion mode resolves to native `auto` scrolling. A `200%` zoom pass retained semantic content and introduced no horizontal overflow. The invitation remains a single line at all five established review widths.

Klause's overprint was checked separately at `768`, `390` and `320px`: removing it changes neither the Patch movement height nor the Rollback/Receipt offsets. At all three widths it intersects both the folder plate and Rollback carrier, proving the accepted seam-crossing while remaining zero-flow.

## Re-inspection questions

- Does Fold 1 establish sufficient professional authority without exhausting the page before the work begins?
- Does Fold 2's right-side breakout and mineral-plane crop now read as serious use changing a strong base system without becoming a second side quest?
- Does Fold 3 now make live refresh and complete-history replay visibly different even before the labels are read, while still making Cache the only source of State?
- Does the equal-height/process-depth construction make the architecture feel authored rather than diagrammed?
- Does State now arrive with enough material presence before its fade becomes the desktop headline seam?
- Does the compact-width left article rail remove the previous arbitrary title/body drift without making the proof feel generic?
- Does the material progression read as ordinary truth → restrained mechanisms → expressive State without becoming western costume?
- Does Writing create enough decompression before Patch returns the page to spectacle?
- Do the transitions around the approved Patch movement preserve its theatricality without making the neighbouring movements feel unfinished?
- Does the reciprocal close feel earned and direct?

## Hard stop

Do not continue into another homepage refinement or into production homepage components, routing, edition selection, animation, publication or final asset commissioning from this handoff. Cloud/Harley Fold 3 material/composition re-inspection is the next gate.
